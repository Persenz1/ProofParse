# Zotero 样本测试结果（2026-09-15）

本轮完成代码修正、本地解析、针对性回归，以及少量真实视觉裁决。没有宣称四篇论文已经逐字校验完毕。

## 测试来源与范围

只读访问用户 Zotero 本地 API 选择附件，并读取 D:/zotero/storage 中已有 PDF。
没有改动 Zotero 条目或附件，没有调用外部多模态 API。

| 论文 | Zotero 条目 / 附件 | 页数 | 图 | 表 | display 公式 | 主要覆盖 |
|---|---|---:|---:|---:|---:|---|
| Instability of a penetrating blade | 9JGR5YV2 / 9YGS96JV | 15 | 11 | 0 | 56 | 数学、力学图、旧文字编码 |
| Bioinspired microrobots | MNKAMYY3 / ZMMPTX44 | 12 | 6 | 0 | 0 | 多子图、双栏长文 |
| Simultaneous localization and mapping: Part I | DXWTRKU6 / EYUUVWY5 | 10 | 7 | 2 | 26 | 公式、表格、参考文献 |
| Robotics Software: Past, Present, and Future | GUWMAKEF / PNMRVKGE | 32 | 3 | 2 | 5 | 长正文、代码、表格 |

图表/公式数量是解析器分类结果，不是人工金标准。机器人软件论文另有 31 个未知类型区域以图片保留；部分代码被上游误认作公式或表格，正是待解决问题。

环境：Python 3.12.14、MinerU 3.4.5、PyTorch 2.14.0+cu126、pypdfium2 5.10.1、RTX 4060 Ti 8GB。
四篇共 69 页均完成本地解析和已定位公式的复核，没有记录到公式阶段异常。
初始待审 239 项：69 全页、85 display、19 inline、62 图表/未知区域、4 文本任务。
本地双路仅做一致性判断；仍有大量公式编号和排版差异需要看图，不以字符相似度冒充正确率。

## 验证过的真实视觉样例

固定 7 个跨 harness 任务中：

- 确认微机器人 Fig.1：a-f 六个面板、标尺和标签完整，裁剪没有夹带正文。
- 确认刀片 Fig.1：弹簧、滑套和 P/B/k/l(P) 标注完整。
- 修正刀片公式 2.1：主体正确，但编号被识别为乱码；查原页后修正 tag。
- 修正机器人软件 Table 1：八行数值、比例核对，恢复两处脚注 a 的上标结构。
- 确认机器人软件的 4×4 数值输出：内容一致，因没有表头保留 HTML 网格，不伪造 Markdown 表头。
- SLAM Table 2 保持 open：Radish 行的“Centre, and more.”被错误并入 IJRR 行，链接字符也需核对。
- 机器人软件第 17 页保持 open：T.log()/Twist3 代码被识别成错误公式，整页不能判通过。

此外，查看刀片论文原始首页，修复了标题被上游归类为页眉的问题：恢复标题位置，并补入标题、作者、年份和 DOI 元数据。修改后的整页仍需重新复查。

固定 7 项的语义结果为：3 确认、2 修正、2 未解决；表格确认也会触发重建 Markdown。
这只是覆盖不同类型的抽样，不是 69 页全量视觉验证。四篇最终均为 still_open。

## 工程检查

11 个回归用例通过：数学结构差异、稳定 ID、长候选、过期裁决、低置信/失败状态、内容保留、表格回退、遗漏段落插入、元数据修正、OCR 空返回、模拟 API 缓存及后续表格修改使整页确认失效等路径（部分用例覆盖多个行为）。
语法与 git diff 空白检查通过；交付中 block_id 唯一，source.pdf 和所引用资源均存在，交付不依赖 E 盘临时目录。
API 只使用模拟响应验证，真实供应商兼容性、费用和 DeepSeek harness 尚未测试。
未记录可归属于本测试的真实 agent token 或标准化冷/热运行时间，因此不报告推算的节省百分比。

## 交付位置

- output/zotero_test：本机 Codex 样本裁决及修正结果。
- output/harness_baseline：相同本地提取逻辑的未裁决基线。
- output/deepseek_test：独立副本，harness_cases.json 指定同一组 7 项。
- docs/HARNESS_TEST.md：给其他 agent 的运行说明。独立裁决前不要阅读本结果文件。

首轮解析后针对实际发现的首页保留/标题选择/DOI 断行问题，对各副本执行了相同的确定性更新，没有为此重复跑 GPU。
PDF 和生成的全文保存在 Git 忽略的本地目录，未提交或推送。

## 已知边界

不保证所有图表检测和公式识别正确；默认不做代数等价证明。重复 inline 字符串无法唯一定位时不自动替换。
跨页内容有相邻上下文，但还没有完整的多区域逐字符映射；复杂裁剪/旋转 PDF 未做真实样本覆盖。
API 的请求数与输出 token 上限不是严格货币预算。当前未实现供应商价格核验和严格金额封顶。
新页面或裁剪由复查补丁改变后必须重新看图。不能用“模型调用结束”替代“内容已经核对完成”。
